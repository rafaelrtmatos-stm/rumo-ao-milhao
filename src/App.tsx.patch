============================================================
PATCH — App.tsx — Persistência de Contratos
Aplicar manualmente: substituir ANTIGO por NOVO em cada bloco
============================================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[1/8] handleDownloadDocx — salvar snapshot ao gerar DOCX (~linha 5295)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ANTIGO:
      URL.revokeObjectURL(url);
      onUpdateVenda({ ...selectedVenda, contratoGerado: true });
    } catch (e: any) {
      alert("Erro ao gerar contrato: " + e.message);
    } finally {
      setDownloadingDocx(false);
    }
  };

NOVO:
      URL.revokeObjectURL(url);
      onUpdateVenda({
        ...selectedVenda,
        contratoGerado: true,
        contratoSnapshot: {
          vendedor: { ...gerarVendedor },
          empreendimento: { ...gerarEmp },
          extra: { ...gerarExtra },
          tipoContrato,
          geradoEm: new Date().toISOString(),
        },
      });
    } catch (e: any) {
      alert("Erro ao gerar contrato: " + e.message);
    } finally {
      setDownloadingDocx(false);
    }
  };


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2/8] handleDownloadPdfContrato — salvar snapshot ao gerar PDF (~linha 5449)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ANTIGO:
      URL.revokeObjectURL(url);
      onUpdateVenda({ ...selectedVenda, contratoGerado: true });
    } catch (e: any) {
      alert("Erro ao gerar PDF do contrato: " + e.message);

NOVO:
      URL.revokeObjectURL(url);
      onUpdateVenda({
        ...selectedVenda,
        contratoGerado: true,
        contratoSnapshot: {
          vendedor: { ...gerarVendedor },
          empreendimento: { ...gerarEmp },
          extra: { ...gerarExtra },
          tipoContrato,
          geradoEm: new Date().toISOString(),
        },
      });
    } catch (e: any) {
      alert("Erro ao gerar PDF do contrato: " + e.message);


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[3/8] handleOpenGerarContratoFormato — pular wizard se há snapshot (~linha 5470)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ANTIGO:
  const handleOpenGerarContratoFormato = (formato: 'pdf' | 'docx') => {
    setGerarFinalFormat(formato);
    handleOpenGerarContrato();
  };

NOVO:
  const handleOpenGerarContratoFormato = (formato: 'pdf' | 'docx') => {
    setGerarFinalFormat(formato);
    // Se já há snapshot salvo, pular direto para preview (step 2)
    if (selectedVenda?.contratoSnapshot) {
      setGerarStep(2);
      setShowGerarModal(true);
    } else {
      handleOpenGerarContrato();
    }
  };


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[4/8] handleOpenGerarContratoForVenda — pré-preencher com snapshot (~linha 5864)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ANTIGO:
  const handleOpenGerarContratoForVenda = (venda: Venda) => {
    const dev = developments.find((d) => d.id === venda.empreendimentoId);
    // Pré-preenche gerarExtra/gerarEmp para quando o usuário quiser baixar depois
    setGerarProprietarioId("");
    setGerarVendedor(emptyGerarVendedor);
    setGerarEmp({
      nome: dev?.nome || "",
      comunidade: dev?.comunidade || "",
      cidade: dev?.cidade || "",
      estado: dev?.estado || "",
    });
    setGerarExtra({
      rua: venda.rua || "",
      comunidade: dev?.comunidade || "",
      formaPagamento: venda.formaPagamento || "Dinheiro",
      medidaFrente: venda.medidaFrente || "",
      medidaLateralDir: venda.medidaLateralDir || "",
      medidaLateralEsq: venda.medidaLateralEsq || "",
      medidaFundos: venda.medidaFundos || "",
      areaTotal: venda.areaTotal || "",
    });
    setTipoContrato(venda.quantidadeParcelas === 0 ? "avista" : "parcelado");
    setGerarStep(0);
    // Apenas abre a visualização — wizard só abre se usuário clicar PDF/DOCX
    setSelectedVenda(venda);
  };

NOVO:
  const handleOpenGerarContratoForVenda = (venda: Venda) => {
    const dev = developments.find((d) => d.id === venda.empreendimentoId);
    const snap = venda.contratoSnapshot;
    setGerarProprietarioId("");
    // Se existe snapshot salvo, restaura dados do momento da geração original
    setGerarVendedor(snap?.vendedor ?? emptyGerarVendedor);
    setGerarEmp(snap?.empreendimento ?? {
      nome: dev?.nome || "",
      comunidade: dev?.comunidade || "",
      cidade: dev?.cidade || "",
      estado: dev?.estado || "",
    });
    setGerarExtra(snap?.extra ?? {
      rua: venda.rua || "",
      comunidade: dev?.comunidade || "",
      formaPagamento: venda.formaPagamento || "Dinheiro",
      medidaFrente: venda.medidaFrente || "",
      medidaLateralDir: venda.medidaLateralDir || "",
      medidaLateralEsq: venda.medidaLateralEsq || "",
      medidaFundos: venda.medidaFundos || "",
      areaTotal: venda.areaTotal || "",
    });
    setTipoContrato(snap?.tipoContrato ?? (venda.quantidadeParcelas === 0 ? "avista" : "parcelado"));
    setGerarStep(0);
    setSelectedVenda(venda);
  };


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[5/8] Lista mobile — botão "Visualizar" verde se há snapshot (~linha 6564)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ANTIGO:
                <button
                  onClick={() => handleOpenGerarContratoForVenda(venda)}
                  className={`flex flex-col items-center gap-1 p-3 rounded-xl shadow-sm border transition-all ${venda.contratoGerado ? "bg-white text-primary-main border-border-subtle hover:bg-primary-main hover:text-white" : "bg-primary-main/10 text-primary-main border-primary-main/20 hover:bg-primary-main hover:text-white"}`}
                >
                  <FileDown size={20} />
                  <span className="text-[9px] font-bold uppercase">{venda.contratoGerado ? "Contrato" : "Gerar"}</span>
                </button>

NOVO:
                <button
                  onClick={() => handleOpenGerarContratoForVenda(venda)}
                  className={`flex flex-col items-center gap-1 p-3 rounded-xl shadow-sm border transition-all ${venda.contratoSnapshot ? "bg-green-50 text-green-700 border-green-200 hover:bg-green-600 hover:text-white" : venda.contratoGerado ? "bg-white text-primary-main border-border-subtle hover:bg-primary-main hover:text-white" : "bg-primary-main/10 text-primary-main border-primary-main/20 hover:bg-primary-main hover:text-white"}`}
                >
                  <FileDown size={20} />
                  <span className="text-[9px] font-bold uppercase">{venda.contratoSnapshot ? "Visualizar" : venda.contratoGerado ? "Contrato" : "Gerar"}</span>
                </button>


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[6/8] Lista desktop — botão "Visualizar" verde se há snapshot (~linha 6619)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ANTIGO:
                  <button
                    onClick={() => handleOpenGerarContratoForVenda(venda)}
                    className={`p-2.5 rounded-xl shadow-sm border transition-all flex items-center gap-1.5 text-xs font-bold ${venda.contratoGerado ? "bg-surface-card text-primary-main border-border-subtle hover:bg-primary-main hover:text-primary-contrast" : "bg-primary-main/10 text-primary-main border-primary-main/20 hover:bg-primary-main hover:text-white"}`}
                    title={venda.contratoGerado ? "Baixar contrato novamente" : "Gerar contrato"}
                  >
                    <FileDown size={15} />
                    {venda.contratoGerado ? "Contrato" : "Gerar Contrato"}
                  </button>

NOVO:
                  <button
                    onClick={() => handleOpenGerarContratoForVenda(venda)}
                    className={`p-2.5 rounded-xl shadow-sm border transition-all flex items-center gap-1.5 text-xs font-bold ${venda.contratoSnapshot ? "bg-green-50 text-green-700 border-green-200 hover:bg-green-600 hover:text-white" : venda.contratoGerado ? "bg-surface-card text-primary-main border-border-subtle hover:bg-primary-main hover:text-primary-contrast" : "bg-primary-main/10 text-primary-main border-primary-main/20 hover:bg-primary-main hover:text-white"}`}
                    title={venda.contratoSnapshot ? "Visualizar contrato salvo" : venda.contratoGerado ? "Baixar contrato novamente" : "Gerar contrato"}
                  >
                    <FileDown size={15} />
                    {venda.contratoSnapshot ? "Visualizar" : venda.contratoGerado ? "Contrato" : "Gerar Contrato"}
                  </button>


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[7/8] Modal de visualização — título, badge e seção verde (~linha 6724)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ANTIGO:
                    <div className="min-w-0">
                      <h4 className="font-display font-bold text-slate-800">Prévia do Contrato</h4>
                      <div className="flex flex-wrap items-center gap-2 mt-1">
                        <p className="text-[10px] font-bold text-slate-400 uppercase tracking-widest truncate">
                          {selectedVenda.numeroContrato}
                        </p>
                        <span className="text-slate-300">•</span>

NOVO:
                    <div className="min-w-0">
                      <h4 className="font-display font-bold text-slate-800">{selectedVenda.contratoSnapshot ? "Contrato Salvo" : "Prévia do Contrato"}</h4>
                      <div className="flex flex-wrap items-center gap-2 mt-1">
                        <p className="text-[10px] font-bold text-slate-400 uppercase tracking-widest truncate">
                          {selectedVenda.numeroContrato}
                        </p>
                        {selectedVenda.contratoSnapshot && (
                          <>
                            <span className="text-slate-300">•</span>
                            <span className="text-[9px] font-bold bg-green-100 text-green-700 px-2 py-0.5 rounded-full">
                              ✓ Dados salvos · {new Date(selectedVenda.contratoSnapshot.geradoEm).toLocaleDateString("pt-BR")}
                            </span>
                          </>
                        )}
                        <span className="text-slate-300">•</span>

---

ANTIGO (seção vendedor + fechamento do modal, ~linha 6910):
                {/* Seller and date */}
                <div className="flex flex-col sm:flex-row gap-3">
                  <div className="flex-1 bg-white rounded-2xl p-4 shadow-sm border border-slate-100">
                    <p className="text-[10px] text-slate-400 uppercase font-bold mb-0.5">Vendedor</p>
                    <p className="font-bold text-slate-800">{selectedVenda.vendedor || "—"}</p>
                  </div>
                  <div className="flex-1 bg-white rounded-2xl p-4 shadow-sm border border-slate-100">
                    <p className="text-[10px] text-slate-400 uppercase font-bold mb-0.5">Data da Venda</p>
                    <p className="font-bold text-slate-800">{new Date(selectedVenda.dataVenda).toLocaleDateString("pt-BR", { day: "2-digit", month: "long", year: "numeric" })}</p>
                  </div>
                </div>
              </div>

NOVO:
                {/* Seller and date */}
                <div className="flex flex-col sm:flex-row gap-3">
                  <div className="flex-1 bg-white rounded-2xl p-4 shadow-sm border border-slate-100">
                    <p className="text-[10px] text-slate-400 uppercase font-bold mb-0.5">Vendedor</p>
                    <p className="font-bold text-slate-800">{selectedVenda.contratoSnapshot?.vendedor?.nome || selectedVenda.vendedor || "—"}</p>
                    {selectedVenda.contratoSnapshot?.vendedor?.cpf && (
                      <p className="text-xs font-mono text-slate-500 mt-0.5">{selectedVenda.contratoSnapshot.vendedor.cpf}</p>
                    )}
                  </div>
                  <div className="flex-1 bg-white rounded-2xl p-4 shadow-sm border border-slate-100">
                    <p className="text-[10px] text-slate-400 uppercase font-bold mb-0.5">Data da Venda</p>
                    <p className="font-bold text-slate-800">{new Date(selectedVenda.dataVenda).toLocaleDateString("pt-BR", { day: "2-digit", month: "long", year: "numeric" })}</p>
                  </div>
                </div>

                {/* Snapshot info: dados complementares do contrato salvo */}
                {selectedVenda.contratoSnapshot && (
                  <div className="bg-green-50 border border-green-200 rounded-2xl p-4 space-y-2">
                    <p className="text-[10px] font-black uppercase tracking-widest text-green-700">Dados do Contrato Gerado</p>
                    <div className="grid grid-cols-2 sm:grid-cols-3 gap-2 text-xs">
                      {selectedVenda.contratoSnapshot.extra?.areaTotal && (
                        <div>
                          <p className="text-green-600 uppercase font-bold mb-0.5">Área Total</p>
                          <p className="font-semibold text-slate-700">{selectedVenda.contratoSnapshot.extra.areaTotal} m²</p>
                        </div>
                      )}
                      {selectedVenda.contratoSnapshot.extra?.rua && (
                        <div>
                          <p className="text-green-600 uppercase font-bold mb-0.5">Logradouro</p>
                          <p className="font-semibold text-slate-700">{selectedVenda.contratoSnapshot.extra.rua}</p>
                        </div>
                      )}
                      {selectedVenda.contratoSnapshot.extra?.formaPagamento && (
                        <div>
                          <p className="text-green-600 uppercase font-bold mb-0.5">Pagamento</p>
                          <p className="font-semibold text-slate-700">{selectedVenda.contratoSnapshot.extra.formaPagamento}</p>
                        </div>
                      )}
                      <div>
                        <p className="text-green-600 uppercase font-bold mb-0.5">Tipo</p>
                        <p className="font-semibold text-slate-700 capitalize">{selectedVenda.contratoSnapshot.tipoContrato === 'avista' ? 'À Vista' : 'Parcelado'}</p>
                      </div>
                      <div>
                        <p className="text-green-600 uppercase font-bold mb-0.5">Gerado em</p>
                        <p className="font-semibold text-slate-700">{new Date(selectedVenda.contratoSnapshot.geradoEm).toLocaleDateString("pt-BR")}</p>
                      </div>
                    </div>
                  </div>
                )}
              </div>


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[8/8] Modal duplicar — "Salvar como novo" limpa snapshot (~linha 7427)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ANTIGO:
                <button
                  onClick={() => {
                    const novoId = `venda-${Date.now()}`;
                    const novoContrato = `CONT-${Date.now()}`;
                    onUpdateVenda({ ...pendingEditVenda, id: novoId, numeroContrato: novoContrato });
                    setEditingVenda(null);
                    setShowDuplicarModal(false);
                    setPendingEditVenda(null);
                  }}
                  className="btn-secondary h-12 font-semibold flex items-center justify-center gap-2"
                >
                  <Copy size={17} /> Duplicar como novo contrato
                </button>

NOVO:
                <button
                  onClick={() => {
                    const novoId = `venda-${Date.now()}`;
                    const novoContrato = `CONT-${Date.now()}`;
                    // Novo contrato: limpa snapshot para forçar nova geração
                    onUpdateVenda({ ...pendingEditVenda, id: novoId, numeroContrato: novoContrato, contratoGerado: false, contratoSnapshot: undefined });
                    setEditingVenda(null);
                    setShowDuplicarModal(false);
                    setPendingEditVenda(null);
                  }}
                  className="btn-secondary h-12 font-semibold flex items-center justify-center gap-2"
                >
                  <Copy size={17} /> Salvar como novo contrato
                </button>
